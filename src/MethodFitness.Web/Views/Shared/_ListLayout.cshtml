@using MethodFitness.Web;
@using HtmlTags
<script>
    @RenderSection("ExtraCss", false)

    @RenderSection("HeaderScripts", false)
    </script>
<script>
    var gridDef=  @MvcHtmlString.Create(JsonUtil.ToJson(Model.GridDef));
    var addEditUrl = "@MvcHtmlString.Create(Model.AddUpdateUrl)";
    var deleteMultipleUrl = "@MvcHtmlString.Create(Model.DeleteMultipleUrl)";
    var contentArea;
    var gridControllerOptions = { gridDef: gridDef,
        addEditUrl: addEditUrl,
        deleteMultipleUrl: deleteMultipleUrl,
        el:"#masterArea"
    };
        @RenderSection("HeaderJavascript", false)
</script>

   <div class="content-header">
        <input class="search" /> 
        @RenderSection("ExtraContentHeaderButtons", false)
        <button id="new" class="new" title="@WebLocalizationKeys.NEW"></button>
        <span class="title-name" data-bind="text:_Title"></span>
    </div>
    <div id="contentArea" class="content-inner">
        @RenderBody()
    </div>
